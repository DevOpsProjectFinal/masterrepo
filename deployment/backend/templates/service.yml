apiVersion: v1
kind: Service
metadata:
  name: backend-api
  namespace: fargate-applications
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: backend-api
