apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  type: {{ .values.service.type }}
  ports:
  - port: {{ .values.service.port }}
    targetPort: 80
  selector:
    app: {{ .Chart.Name }}
